{
  "name": "heroku_app",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "node server/bin/www",
    "heroku-postbuild": "cd frontend && npm install && npm run build && cd .. && cd server && npm install"
  },
  "engines": {
    "node": "14.x"
  },
  "dependencies": {
    "d3": "^5.16.0",
    "d3-hierarchy": "^1.1.9",
    "react-d3-tree": "^1.16.1"
  },
  "devDependencies": {
    "@types/d3": "^5.7.2"
  }
}
